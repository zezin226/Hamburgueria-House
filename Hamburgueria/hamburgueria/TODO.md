# TODO - Adicionar imagens nos cards

- [x] Atualizar `js/dados.js` adicionando campo `imagem` em cada item do cardápio.
- [x] Atualizar `js/app.js` para renderizar `<img>` dentro de cada card usando o campo `imagem`.
- [x] Atualizar `css/style.css` com estilos para `.card-imagem` garantindo layout consistente.
- [ ] Testar no navegador se as imagens carregam e se o layout permanece responsivo.



